<form class="form-inline" action="{{ path(searchPath) }}">
    <div class="form-group">
        <label for="title"></label>
        <input type="text" class="form-control" name="keyword" placeholder="Raktažodis" value="{{ keyword ?? '' }}">
        <input type="hidden" name="_token" value="{{ csrf_token('searchAds') }}">
    </div>
    <button type="submit" class="btn btn-default">Ieškoti</button>
</form>
<hr>