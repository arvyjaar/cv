{% extends 'base.html.twig' %}

{% block body %}
    <h2>Redaguok skelbimą!</h2>

    {{ form_start(edit_form) }}

        {{ form_label(edit_form.title, null, {'label': 'Siūloma pozicija*'}) }}
        {{ form_row(edit_form.title) }}

        {{ form_label(edit_form.description, null, {'label': 'Apie siūlomą poziciją*'}) }}
        {{ form_row(edit_form.description) }}

        {{ form_label(edit_form.assigment, null, {'label': 'Nuoroda į užduotį*'}) }}
        {{ form_row(edit_form.assignment) }}

        {#<label for="form_requirements">reikalavimai</label>#}
        {#{{ form_row(edit_form.requirements) }}#}

        <div class="text-center">
            <input class="btn btn-primary" type="submit" value="Sukurti"/>
        </div>
        {{ form_start(delete_form) }}
            <div class="text-center">
                <input  class="btn btn-default" type="submit" value="Trinti">
            </div>
        {{ form_end(delete_form) }}

    {{ form_end(edit_form) }}

    <a class="pull-right" href="{{ path('jobad_my_index') }}">Atgal į sąrašą</a>

{% endblock %}
